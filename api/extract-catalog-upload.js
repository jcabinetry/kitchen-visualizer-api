import { setCorsHeaders } from "./_lib/cors.js";
import { saveCatalog, cleanCatalogId } from "./_lib/catalogStore.js";
import pdfParse from "pdf-parse";

function slug(v){return String(v||"").toLowerCase().trim().replace(/[^a-z0-9]+/g,"-").replace(/-+/g,"-").replace(/^-|-$/g,"").slice(0,90)}
function admin(req,res){const t=process.env.ADMIN_API_TOKEN||process.env.ADMIN_TOKEN||"";if(!t)return true;const s=req.headers["x-admin-token"]||req.headers.authorization?.replace(/^Bearer\s+/i,"");if(s===t)return true;res.status(401).json({error:"Unauthorized."});return false}
function pdfBuffer(v){return Buffer.from(String(v||"").replace(/^data:application\/pdf;base64,/,""),"base64")}
function json(text){const raw=String(text||"").trim();try{return JSON.parse(raw)}catch(e){}const m=raw.match(/\{[\s\S]*\}/);if(!m)throw new Error("AI did not return JSON.");return JSON.parse(m[0])}
function responseText(data){if(data?.output_text)return data.output_text;const parts=[];(data?.output||[]).forEach(o=>(o.content||[]).forEach(c=>{if(typeof c.text==="string")parts.push(c.text);if(typeof c.output_text==="string")parts.push(c.output_text)}));return parts.join("\n")||JSON.stringify(data)}

async function uploadToOpenAI(buffer,name){
  const form=new FormData();form.append("purpose","user_data");form.append("file",new Blob([buffer],{type:"application/pdf"}),name||"catalog.pdf");
  const r=await fetch("https://api.openai.com/v1/files",{method:"POST",headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`},body:form});
  const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d?.error?.message||"PDF upload to AI failed.");return d.id;
}
function pickCatalogLines(text){const lines=String(text||"").replace(/\r/g,"\n").split(/\n+/).map(x=>x.replace(/\s+/g," ").trim()).filter(Boolean);const key=/door|style|finish|finishes|color|colour|paint|stain|species|wood|maple|oak|cherry|hickory|alder|birch|walnut|thermofoil|laminate|mdf|hdf|collection|classic|base|wall|available|selection|palette|panel|shaker|slab|raised|recessed|merillat|aristokraft|cabdoor/i;const picked=[];lines.forEach((line,i)=>{if(key.test(line))picked.push(lines[i-3],lines[i-2],lines[i-1],line,lines[i+1],lines[i+2],lines[i+3])});return [lines.slice(0,400).join("\n"),Array.from(new Set(picked.filter(Boolean))).join("\n"),lines.slice(-250).join("\n")].join("\n\n").slice(0,120000)}
async function getPdfText(pdfs){const out=[];for(let i=0;i<pdfs.length;i++){const b=pdfBuffer(pdfs[i].fileBase64);try{const parsed=await pdfParse(b);out.push(`PDF ${i+1}: ${pdfs[i].fileName||"catalog.pdf"}\nTEXT_CHARS:${String(parsed.text||"").length}\n${pickCatalogLines(parsed.text||"")}`)}catch(e){out.push(`PDF ${i+1}: ${pdfs[i].fileName||"catalog.pdf"}\nTEXT EXTRACTION FAILED: ${e.message}`)}}return out.join("\n\n---\n\n")}

async function aiJson(prompt, content=null){const body=content?{model:process.env.CATALOG_EXTRACT_MODEL||"gpt-4.1",input:[{role:"user",content}],max_output_tokens:20000}:{model:process.env.CATALOG_EXTRACT_MODEL||"gpt-4.1",input:prompt,max_output_tokens:20000};const r=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${process.env.OPENAI_API_KEY}`},body:JSON.stringify(body)});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d?.error?.message||"AI extraction failed.");return json(responseText(d))}

async function runTextAI(name,pdfs,reason){const text=await getPdfText(pdfs);const prompt=`Build a cabinet catalog from this PDF text. You MUST extract at least one line if there are any cabinet door, finish, color, stain, paint, species, or material names in the text. Return JSON only: {"lines":[{"name":"collection","description":"","doors":[{"name":"door style","description":"","image":"","imageStatus":"needs-image"}],"finishes":[{"name":"finish/color","description":"","swatch":"","swatchStatus":"needs-image"}],"species":["species"]}],"imagePlan":[],"notes":""}. Manufacturer: ${name}. Fallback reason: ${reason}. Text:\n${text}`;return {data:await aiJson(prompt),fileIds:[],method:"pdf-text"}}

async function runFileAI(name,pdfs){if(!process.env.OPENAI_API_KEY)throw new Error("OPENAI_API_KEY is not configured.");const content=[{type:"input_text",text:`Extract a cabinet catalog from the attached PDFs. Cross-reference all PDFs. Return JSON only: {"lines":[{"name":"collection","description":"","doors":[{"name":"door style","description":"","image":"","imageStatus":"needs-image"}],"finishes":[{"name":"finish/color","description":"","swatch":"","swatchStatus":"needs-image"}],"species":["species"]}],"imagePlan":[{"type":"door or swatch","name":"item name","pdf":"file name","page":"page number","hint":"where the picture appears"}],"notes":""}. Extract all visible door styles, finish colors, stains, paints, species, materials, and image locations. Manufacturer: ${name}.`}];const ids=[];for(let i=0;i<pdfs.length;i++){const b=pdfBuffer(pdfs[i].fileBase64);if(!b.length)throw new Error((pdfs[i].fileName||"PDF")+" is empty.");if(b.slice(0,4).toString()!=="%PDF")throw new Error((pdfs[i].fileName||"PDF")+" is not a valid PDF.");const id=await uploadToOpenAI(b,pdfs[i].fileName||`catalog-${i+1}.pdf`);ids.push(id);content.push({type:"input_text",text:`PDF ${i+1}: ${pdfs[i].fileName||`catalog-${i+1}.pdf`}`});content.push({type:"input_file",file_id:id})}return {data:await aiJson(null,content),fileIds:ids,method:"file"}}

function heuristic(name,text){const candidates=Array.from(new Set(String(text||"").match(/\b[A-Z][A-Za-z0-9'&. -]{2,36}\b/g)||[]));const stop=/^(The|And|For|With|Door|Doors|Style|Styles|Finish|Finishes|Color|Colors|Cabinet|Cabinets|Kitchen|Classic|Selection|Guide|Page|Table|Contents|Available|Shown|See|Also|All|New|Use|Base|Wall|PDF|TEXT|CHARS)$/i;const finish=/white|gray|grey|black|brown|natural|stain|paint|maple|oak|cherry|hickory|alder|birch|walnut|pecan|cognac|espresso|slate|linen|cotton|dove|moon|cappuccino|cider|clove|java|mist|sage|navy|blue|sand|burlap|fawn|toast|umber|cocoa|vanilla|cream|almond|chiffon|pebble|riverbed|driftwood/i;const door=/shaker|slab|raised|recessed|panel|arch|square|mission|miter|bead|cathedral|flat|overlay|drawer/i;const finishes=candidates.filter(x=>finish.test(x)&&!stop.test(x)).slice(0,180).map(x=>({name:x,description:"Catalog finish",swatch:"",swatchStatus:"needs-image"}));const doors=candidates.filter(x=>door.test(x)&&!stop.test(x)&&!finish.test(x)).slice(0,180).map(x=>({name:x,description:"Catalog door style",image:"",imageStatus:"needs-image"}));const species=Array.from(new Set((String(text||"").match(/\b(Maple|Oak|Cherry|Hickory|Alder|Birch|Walnut|Thermofoil|Laminate|MDF|HDF)\b/gi)||[]).map(v=>v[0].toUpperCase()+v.slice(1).toLowerCase())));return {lines:[{name:"Catalog Extracted",description:"Automatic fallback extraction",doors,finishes,species}],imagePlan:[],notes:`Heuristic fallback used for ${name}.`}}

function normalize(name,data){return (Array.isArray(data?.lines)?data.lines:[]).map((line,i)=>{const lineName=String(line.name||`Line ${i+1}`).trim();return {id:slug(lineName)||`line-${i+1}`,name:lineName,description:String(line.description||"").trim(),doors:(Array.isArray(line.doors)?line.doors:[]).map(x=>{const label=String(x.name||x.label||"").trim();return {id:slug(label),label,value:`${name} ${label} cabinet door style`,image:x.image||"",thumbnail:x.image||"",imageStatus:x.image?"ready":(x.imageStatus||"needs-image"),desc:String(x.description||"Catalog door style").trim()}}).filter(x=>x.label),finishes:(Array.isArray(line.finishes)?line.finishes:[]).map(x=>{const label=String(x.name||x.label||"").trim();const img=x.swatch||x.image||"";return {id:slug(label),label,value:`${name} ${label} cabinet finish`,image:img,swatch:img,thumbnail:img,swatchStatus:img?"ready":(x.swatchStatus||"needs-image"),desc:String(x.description||"Catalog finish").trim()}}).filter(x=>x.label),species:Array.isArray(line.species)?Array.from(new Set(line.species.map(String).filter(Boolean))):[]}}).filter(l=>l.doors.length||l.finishes.length||l.species.length)}

export default async function handler(req,res){if(setCorsHeaders(req,res,"POST, OPTIONS"))return;res.setHeader("Cache-Control","no-store, max-age=0");if(req.method!=="POST")return res.status(405).json({error:"Method not allowed."});if(!admin(req,res))return;try{const body=req.body||{};const name=String(body.name||"Imported Catalog").trim();const catalogId=cleanCatalogId(body.catalogId||slug(name));const pdfs=Array.isArray(body.pdfs)?body.pdfs:[];if(!catalogId)throw new Error("Manufacturer name is required.");if(!pdfs.length)throw new Error("Upload one or more PDFs.");let extracted=await runFileAI(name,pdfs);let lines=normalize(name,extracted.data);if(!lines.length){extracted=await runTextAI(name,pdfs,"file extraction returned zero lines");lines=normalize(name,extracted.data)}if(!lines.length){const text=await getPdfText(pdfs);extracted={data:heuristic(name,text),fileIds:[],method:"heuristic"};lines=normalize(name,extracted.data)}if(!lines.length)throw new Error("Extraction failed: this PDF appears image-only or unreadable. Next required build is PDF page-image vision/cropping.");const sourceUrls=pdfs.map((p,i)=>p.fileName||`PDF ${i+1}`);const catalog=await saveCatalog({catalogId,name,version:new Date().toISOString().slice(0,10),sourceType:"uploaded-pdfs",sourceUrl:sourceUrls[0]||"uploaded-pdf",sourceUrls,notes:extracted.data.notes||"Uploaded PDFs extracted.",extraction:{engine:"v3-upload",status:"ready-needs-images",updatedAt:new Date().toISOString(),method:extracted.method,imagePlan:extracted.data.imagePlan||[]},stats:{sources:pdfs.length,pdfSources:pdfs.length,lines:lines.length,doors:lines.reduce((s,l)=>s+l.doors.length,0),finishes:lines.reduce((s,l)=>s+l.finishes.length,0),species:Array.from(new Set(lines.flatMap(l=>l.species||[]))).length},manufacturers:[{id:slug(name),name,sourceUrl:sourceUrls[0]||"uploaded-pdf",sourceUrls,lines}]});res.status(200).json({catalog,message:"Catalog extracted. Door and swatch images are marked for the next image step."})}catch(e){res.status(400).json({error:e?.message||"Catalog extraction failed."})}}